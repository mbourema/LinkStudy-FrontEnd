import { useEffect } from "react";

export default function Ads() {
    useEffect(() => {
      document.title = "Ads - LinkStudy";
    }, []);

    return (
        <div>
            
        </div>
    );
}