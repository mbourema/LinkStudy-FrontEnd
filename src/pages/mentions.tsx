import { useEffect } from "react";

export default function Mentions() {
    useEffect(() => {
      document.title = "Mentions - LinkStudy";
    }, []);

    return (
        <div>
            
        </div>
    );
}