import { useEffect } from "react";

export default function Inscription() {
    useEffect(() => {
      document.title = "Inscription - LinkStudy";
    }, []);

    return (
        <div>
            
        </div>
    );
}