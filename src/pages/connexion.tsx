import { useEffect } from "react";

export default function Connexion() {
    useEffect(() => {
      document.title = "Connexion - LinkStudy";
    }, []);

    return (
        <div>
            
        </div>
    );
}